// src/blogger/utils/htmlToBlocks.js

export function htmlToBlocks(html) {
  // Convert HTML to editor blocks (for TipTap)
  // This is a simplified version
  return html;
}

export function blocksToHtml(blocks) {
  // Convert editor blocks to HTML
  return blocks;
}